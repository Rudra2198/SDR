"""Plane 03 — Capability (agents).

The three Claude judgment steps via the Anthropic SDK: write email (narration),
classify reply (reasoning), draft response (reasoning). Judgment only — Claude holds
no state and never decides control flow. Built from Phase 2 onward.
"""
